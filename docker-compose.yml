version: '3.4'

services:
  db:
    image: "mysql/mysql-server:latest"
    ports:
      - "3306:3306"
    environment:
        MYSQL_ROOT_HOST: "%"
        MYSQL_ROOT_PASSWORD: "${DB_PASSWORD}"
        MYSQL_DATABASE: "${DB_DATABASE}"
        MYSQL_USER: "${DB_USERNAME}"
        MYSQL_PASSWORD: "${DB_PASSWORD}"
    networks:
      - "webshop"
    volumes:
      - "db_data:/var/lib/mysql"
  phpmyadmin:
    image: "phpmyadmin:latest"
    ports:
      - "8090:80"
    environment:
      PMA_HOST: "db"
      MYSQL_ROOT_PASSWORD: "${DB_PASSWORD}"
    networks:
      - "webshop"
    depends_on:
      - "db"
  mailhog:
    image: "mailhog/mailhog:latest"
    ports:
      - "1025:1025"
      - "8025:8025"
    networks:
      - "webshop"
#  webshop:
#    build:
#      context: "."
#      dockerfile: "Dockerfile"
#    image: "webshop:latest"
#    ports:
#      - "8080:8080"
#    environment:
#      SPRING_DATASOURCE_URL: "jdbc:mysql://db:3306/webshop"
#      SPRING_DATASOURCE_USERNAME: "${DB_USERNAME}"
#      SPRING_DATASOURCE_PASSWORD: "${DB_PASSWORD}"
#      SPRING_JPA_DATABASE_PLATFORM: "org.hibernate.dialect.MySQL8Dialect"
#      SPRING_JPA_GENERATE-DDL: "true"
#      SPRING_JPA_HIBERNATE_DDL-AUTO: "update"
#      SPRING_JPA_DEFER-DATASOURCE_INITIALIZATION: "true"
#      SPRING_SQL_INIT_MODE: "always"
#      SPRING_SQL_INIT_DATA-LOCATIONS: "classpath:database/data.sql"
#      SPRING_MAIL_HOST: "mailhog"
#      SPRING_MAIL_PORT: "1025"
#
#    networks:
#      - "webshop"
#    depends_on:
#      - "db"

networks:
  webshop:
    driver: bridge
volumes:
  db_data:
    driver: local